# === 1. 认知蒸馏模块 (Cognitive Distillation) ===
# 负责观察、分析、总结和提取知识 (通常需要更高智商的模型，如 GPT-4o)
DISTILLATION_OPENAI_API_KEY=sk-...
DISTILLATION_OPENAI_BASE_URL=https://api.openai.com/v1
DISTILLATION_MODEL=gpt-4o

# === 2. 执行模块 (Execution) ===
# 负责具体任务执行、代码生成等 (可以使用更快的模型)
EXECUTION_OPENAI_API_KEY=sk-...
EXECUTION_OPENAI_BASE_URL=https://api.openai.com/v1
EXECUTION_MODEL=gpt-4o

# === 3. Browser Use Cloud (可选) ===
# 如果你希望用云浏览器/抗指纹
BROWSER_USE_API_KEY=

# === 4. 通用参数 (General) ===
# LLM 请求参数
EXOGRAM_LLM_TEMPERATURE=0.0
EXOGRAM_LLM_MAX_TOKENS=16384
EXOGRAM_OPENAI_TIMEOUT=120
EXOGRAM_OPENAI_MAX_RETRIES=3

# 数据目录
EXOGRAM_DATA_DIR=./data
